name: test_app_with_testing
description: A comprehensive test app to verify testing infrastructure
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter: sdk: '>=3.0.0 <4.0.0'
  provider: ^6.0.0
  shared_preferences: ^2.0.0
  http: ^0.13.0
  sqflite: ^2.0.0
  path: ^1.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
